# ── Dependencies ──
node_modules/
.pnp
.pnp.js

# ── Build artefacts ──
dist/
build/
.next/
.vite/

# ── Env files (NEVER commit secrets) ──
.env
.env.local
.env.*.local
*.pem

# ── Logs ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ── Editors ──
.vscode/
.idea/
*.swp
*.swo

# ── OS ──
.DS_Store
Thumbs.db

# ── Prisma generated client / local migrations data ──
/backend/prisma/migrations/migration_lock.toml.bak

# ── Coverage / test output ──
coverage/
.nyc_output/

# ── Local TS build info ──
*.tsbuildinfo

# (login video and prototype HTML are kept — they're project assets, not generated)
